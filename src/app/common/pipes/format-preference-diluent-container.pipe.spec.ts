import { FormatPreferenceDiluentContainerPipe } from "./format-preference-diluent-container.pipe";

describe('FormatPreferenceDiluentContainerPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPreferenceDiluentContainerPipe();
    expect(pipe).toBeTruthy();
  });
});
