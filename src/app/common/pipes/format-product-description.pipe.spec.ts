import { FormatProductDescriptionPipe } from './format-product-description.pipe';

describe('FormatProductDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatProductDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
