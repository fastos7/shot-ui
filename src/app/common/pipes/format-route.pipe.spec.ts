import { FormatRoutePipe } from './format-route.pipe';

describe('FormatRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
