import { FormatPreferenceExactPipe } from './format-preference-exact.pipe';

describe('FormatPreferenceExactPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPreferenceExactPipe();
    expect(pipe).toBeTruthy();
  });
});
