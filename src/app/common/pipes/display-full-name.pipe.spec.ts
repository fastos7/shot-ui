import { DisplayFullNamePipe } from './display-full-name.pipe';

describe('DisplayFullNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayFullNamePipe();
    expect(pipe).toBeTruthy();
  });
});
