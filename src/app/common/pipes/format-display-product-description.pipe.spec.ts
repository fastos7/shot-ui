import { FormatDisplayProductDescriptionPipe } from './format-display-product-description.pipe';

describe('FormatDisplayProductDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDisplayProductDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
