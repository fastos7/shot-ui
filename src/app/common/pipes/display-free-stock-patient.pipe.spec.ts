import { DisplayFreeStockPatientPipe } from './display-free-stock-patient.pipe';

describe('DisplayFreeStockPatientPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayFreeStockPatientPipe();
    expect(pipe).toBeTruthy();
  });
});
