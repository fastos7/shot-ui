import { FormatPreferenceDoseRangePipe } from './format-preference-dose-range.pipe';

describe('FormatPreferenceDoseRangePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPreferenceDoseRangePipe();
    expect(pipe).toBeTruthy();
  });
});
