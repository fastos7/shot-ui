import { DisplayBatchModifiedByUserPipe } from './display-batch-modified-by-user.pipe';

describe('DisplayBatchModifiedByUserPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayBatchModifiedByUserPipe();
    expect(pipe).toBeTruthy();
  });
});
