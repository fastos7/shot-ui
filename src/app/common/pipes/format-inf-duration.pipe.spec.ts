import { FormatInfDuration } from './format-inf-duration.pipe';

describe('FormatInfDuration', () => {
  it('create an instance', () => {
    const pipe = new FormatInfDuration();
    expect(pipe).toBeTruthy();
  });
});
